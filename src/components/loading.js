import React, { Component } from 'react';
import loadingimg from '../images/loading.gif';
const Loading = () =>	{
			return (
                <img src={loadingimg}/>
            );
}
export default Loading;
